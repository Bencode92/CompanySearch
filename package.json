{
  "name": "interim-dirigeants-1962",
  "private": true,
  "version": "1.0.0",
  "description": "Extraction boîtes d'intérim (NAF 78.20Z) avec dirigeants nés avant 1963 -> CSV",
  "scripts": {
    "build": "node scripts/build_pappers.js",
    "fetch:free": "node scripts/fetch_sirens_gouv.js",
    "enrich": "node scripts/enrich_from_list.js",
    "build:cheap": "npm run fetch:free && npm run enrich"
  },
  "dependencies": {
    "axios": "^1.7.4",
    "dotenv": "^16.4.5"
  }
}